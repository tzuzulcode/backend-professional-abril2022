{
  "name": "users",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"npx prisma migrate dev && node ./src/index.js",
    "start": "prisma migrate dev --create-only && npx prisma migrate deploy && npx prisma generate && node ./src/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^3.14.0",
    "express": "^4.18.1",
    "prisma": "^3.14.0"
  }
}
